# Changesets

This folder manages release notes and versioning via Changesets.

- Create a new release note with `pnpm release`
- Version packages with `pnpm version-packages`
- Publishing in CI uses the GitHub Action workflow
