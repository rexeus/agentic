---
"@rexeus/agentic": patch
---

Add OpenCode umbrella support with plugin hooks, installer CLI, and global `agentic-*` command assets.

- expose OpenCode plugin entry and `agentic` CLI from the package
- add install, uninstall, and doctor flows for OpenCode config
- add dedicated OpenCode command templates and guardrail checks
